// ------------------------- Local Storage -------------------------

// local storage is eb storage object which are not sent to server with each request.
// this data survives a full page refresh and even a full browser restart.

// to store data in form of key and value

// localStorage.setItem("","");
// first argument as key
// second argument as value

// we can see local storage in browser (Applications --> LocalStorage)


// -------------- LocalStorage Method --------------

// localStorage.setItem(key , value)     --> Store key/value pair
// localStorage.getItem(key)             --> get the value by key
// localStorage.removeItem(key)          --> remove the key with it's value
// localStorage.clear()                  --> delete everything
// localStorage.key(index)               --> get the key on given position
// localStorage.length                   --> number of stored items
